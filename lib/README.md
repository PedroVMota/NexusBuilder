# External Libraries

Place your external libraries (.lib, .a, .dll, .so files) in this directory.

For header-only libraries, you can create subdirectories here and include them in your premake5.lua configuration.